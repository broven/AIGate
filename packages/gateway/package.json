{
  "name": "gateway",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --env-file ../../.env --watch src/index.ts",
    "start": "bun run --env-file ../../.env src/index.ts",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "seed": "bun run --env-file ../../.env src/scripts/seed-benchmark-data.ts"
  },
  "dependencies": {
    "@aigate/shared": "workspace:*",
    "drizzle-orm": "^0.39.0",
    "@musistudio/llms": "^1.0.53",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0"
  }
}
